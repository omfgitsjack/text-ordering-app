(function () {
    "use strict";

    angular
        .module('utilities.entitymanager')
        .run(entityManagerConfig);

    // minimal configuration
    function entityManagerConfig(breeze) {

    }

})();