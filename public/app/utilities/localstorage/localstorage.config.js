(function () {
    "use strict";

    angular
        .module('utilities.localstorage')
        .config(localStorageConfig);

    function localStorageConfig(localStorageServiceProvider)
    {
        // Put whatever future configs here
    }

})();
